import 'package:flutter/material.dart';



class Init4Page extends StatelessWidget {
  const Init4Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
        body: const Center(
          child: Text('Hello World'),
        ),
    );
  }
}